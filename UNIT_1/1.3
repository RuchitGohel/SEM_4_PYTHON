#1.3 Write a program to input Principal amount,  rate of interest and years to display simple interest

principal = float(input("Enter principal amount"))
rate = float(input("Enter rate of interest"))
n = float(input("Enter numbre of years"))

SI = (principal*rate*n) / 100

print("Simple interest is :" , SI)
